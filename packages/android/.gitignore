# Whitelist: only the TWA config and docs are versioned.
# Everything else (Android project generated by `bubblewrap update`,
# keystore, build outputs) is regenerable or secret.
*
!.gitignore
!twa-manifest.json
!README.md
!patch-monochrome.sh
